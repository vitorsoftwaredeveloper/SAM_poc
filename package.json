{
  "name": "sam-hello",
  "version": "1.0.0",
  "scripts": {
    "build": "sam build",
    "local": "sam local start-api",
    "deploy": "sam deploy --guided"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.138",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "esbuild": "^0.25.10"
  }
}
